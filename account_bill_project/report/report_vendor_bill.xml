<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data>

<template id="report_vendor_bill_project"
          inherit_id="account.report_invoice_document">

    <!-- Ganti header kolom Description → Akun COA -->
    <xpath expr="//th[@name='th_description']" position="replace">
        <th name="th_description" class="text-start">
            <span>Akun COA</span>
        </th>
    </xpath>

    <!-- Ganti isi kolom deskripsi → tampilkan COA code + name -->
    <xpath expr="//td[@name='account_invoice_line_name']" position="replace">
        <td name="account_invoice_line_name">
            <t t-if="line.account_id">
                <strong><t t-esc="line.account_id.code"/></strong>
                <span> - <t t-esc="line.account_id.name"/></span>
            </t>
            <t t-if="line.name">
                <br/><small style="color:#888;"><t t-esc="line.name"/></small>
            </t>
        </td>
    </xpath>

    <!-- Tambah Project / Analytic setelah div informations -->
    <xpath expr="//div[@id='informations']" position="after">
        <t t-if="o.x_analytic_account_id">
            <div style="margin:4px 0 8px 0;padding:6px 12px;background:#e6f7f8;border-left:3px solid #017e84;">
                <strong style="color:#015f64;">Project: </strong>
                <span t-field="o.x_analytic_account_id.name" style="color:#015f64;"/>
                <span style="color:#017e84;margin-left:12px;">
                    Alokasi: <t t-esc="o.x_analytic_distribution_pct"/>%
                </span>
            </div>
        </t>
    </xpath>

    <!-- Tambah info approval setelah div total -->
    <xpath expr="//div[@id='total']" position="after">
        <t t-if="o.x_approval_state == 'approved' and o.x_approved_by">
            <div style="margin-top:12px;padding:8px 12px;border:1px solid #dee2e6;border-radius:4px;font-size:12px;">
                <strong>Disetujui oleh: </strong>
                <span t-field="o.x_approved_by.name"/>
                <span style="margin-left:16px;color:#6c757d;">
                    <t t-esc="o.x_approved_date" t-options='{"widget":"datetime"}'/>
                </span>
            </div>
        </t>
    </xpath>

</template>

</data>
</odoo>
