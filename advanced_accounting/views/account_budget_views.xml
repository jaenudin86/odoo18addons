<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <!-- Budget Tree View -->
    <record id="view_account_budget_tree" model="ir.ui.view">
        <field name="name">account.budget.tree</field>
        <field name="model">account.budget</field>
        <field name="arch" type="xml">
            <tree string="Budgets" decoration-info="state=='draft'" decoration-success="state=='approved'">
                <field name="name"/>
                <field name="code"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="total_planned" widget="monetary"/>
                <field name="total_practical" widget="monetary"/>
                <field name="total_percentage" widget="percentage"/>
                <field name="state" widget="badge" decoration-info="state=='draft'" decoration-success="state in ['confirmed','approved']" decoration-danger="state=='cancelled'"/>
            </tree>
        </field>
    </record>

    <!-- Budget Form View -->
    <record id="view_account_budget_form" model="ir.ui.view">
        <field name="name">account.budget.form</field>
        <field name="model">account.budget</field>
        <field name="arch" type="xml">
            <form string="Budget">
                <header>
                    <button name="action_confirm" string="Confirm" type="object" 
                            invisible="state != 'draft'" class="btn-primary"/>
                    <button name="action_approve" string="Approve" type="object" 
                            invisible="state != 'confirmed'" class="btn-primary" groups="advanced_accounting.group_account_budget_manager"/>
                    <button name="action_done" string="Done" type="object" 
                            invisible="state != 'approved'" class="btn-primary"/>
                    <button name="action_cancel" string="Cancel" type="object" 
                            invisible="state in ['done','cancelled']"/>
                    <button name="action_draft" string="Set to Draft" type="object" 
                            invisible="state != 'cancelled'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,approved,done"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Budget Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="currency_id" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Budget Lines">
                            <field name="budget_line_ids">
                                <tree editable="bottom">
                                    <field name="analytic_account_id"/>
                                    <field name="general_account_id"/>
                                    <field name="date_from" invisible="1"/>
                                    <field name="date_to" invisible="1"/>
                                    <field name="planned_amount" widget="monetary" sum="Total Planned"/>
                                    <field name="practical_amount" widget="monetary" sum="Total Practical"/>
                                    <field name="theoretical_amount" widget="monetary"/>
                                    <field name="percentage" widget="percentage"/>
                                    <field name="currency_id" column_invisible="1"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Summary">
                            <group>
                                <group>
                                    <field name="total_planned" widget="monetary"/>
                                    <field name="total_practical" widget="monetary"/>
                                </group>
                                <group>
                                    <field name="total_percentage" widget="percentage"/>
                                </group>
                            </group>
                        </page>
                        <page string="Notes">
                            <field name="note"/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- Budget Search View -->
    <record id="view_account_budget_search" model="ir.ui.view">
        <field name="name">account.budget.search</field>
        <field name="model">account.budget</field>
        <field name="arch" type="xml">
            <search string="Budgets">
                <field name="name"/>
                <field name="code"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Confirmed" name="confirmed" domain="[('state','=','confirmed')]"/>
                <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
                <group expand="0" string="Group By">
                    <filter string="Status" name="group_state" context="{'group_by':'state'}"/>
                    <filter string="Start Date" name="group_date_from" context="{'group_by':'date_from'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Budget Action -->
    <record id="action_account_budget" model="ir.actions.act_window">
        <field name="name">Budgets</field>
        <field name="res_model">account.budget</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new budget
            </p>
            <p>
                A budget is a forecast of your company's income and expenses expected for a period in the future.
            </p>
        </field>
    </record>

</odoo>
